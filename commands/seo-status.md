---
description: Where this site is in the SEO pipeline, and the one command to run next.
---

Run `seo status` from the user's project directory and read the result back to them plainly.

Then act on the `next:` line: explain what that stage will do and why it is next, and offer to
run it. If the next step is a human gate, say exactly what the user has to look at and decide.
Do not set a `confirmed_at` yourself.
